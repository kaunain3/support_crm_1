# This project currently has no required secrets.
# PORT is provided automatically by Railway in production.
# Uncomment/add variables here if you extend the project (e.g., a hosted Postgres URL).
# DATABASE_URL=
